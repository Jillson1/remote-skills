# Skill 元数据（仅供网站展示使用，不会被 AI 加载到上下文）

workflow:
  name: "PDF 用户反馈周报生成流程"
  input: "关注周日期区间（必填），可选对比周日期、数据源 URL、关键词筛选"
  steps:
    - "📥 Step 1 数据准备：读取源数据并导出【关注周】、【对比周】数据"
    - "📊 Step 2 分析报告：统计关键指标生成结构化报告"
    - "🌐 Step 3 发布：使用 AirpageFile 将报告发布为 WPS 在线智能文档，返回分享链接"
  output: "PDF 用户反馈周报（Markdown 文件 + WPS 在线文档链接）"
